import React from 'react'

function Form({ setInputDate }) {

  

  return (
    <div>
      <h1>Select a date</h1>
      <input type="date"/>
      <button>Submit</button>
    </div>
  )
}

export default Form
